# llama-cpp-python with CUDA support — install with:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --no-cache-dir
# (on Windows PowerShell: $env:CMAKE_ARGS="-DGGML_CUDA=on"; pip install llama-cpp-python --no-cache-dir)
# Falls back to CPU-only if CUDA toolkit isn't set up — still works, just slower.
llama-cpp-python>=0.2.90
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
redis>=5.0.0
httpx>=0.27.0
huggingface_hub>=0.23.0
streamlit>=1.35.0
pandas>=2.2.0